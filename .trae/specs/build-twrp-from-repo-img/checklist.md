- [x] `.github/workflows/blank.yml` 中 `IMG_URL` 改为可选，`BUILD_TWRP` 新增布尔输入默认 false
- [x] 当 `IMG_URL` 为空时，workflow 直接使用 checkout 出的 `/workspace/recovery.img`，不执行 wget
- [x] 设备树生成步骤行为与原 workflow 一致，仍产出 `DeviceTree_<run_number>.zip` 上传到 Release
- [x] 新增 `build-twrp` job，`needs: generate-device-tree` 且仅当 `BUILD_TWRP=true` 时运行
- [x] 编译 job 把 device tree 注入 TWRP 源码树并执行 `mka recoveryimage`
- [x] 编译产物重命名为 `TWRP_<device>_<run_number>.img` 上传到同一个 Release
- [x] 设备树 job 与编译 job 解耦：编译失败不影响 device tree release
- [x] 编译 job 设置 `timeout-minutes: 360`，末尾有 `if: always()` 诊断步骤
- [x] README 末尾追加说明（不修改已有内容），含两个产物命名规则与降级方式
- [x] YAML 语法合法（`yaml.safe_load` 通过）
- [x] `git diff` 确认仅修改 `.github/workflows/blank.yml` 与 `README.md`，未动 `recovery.img` / `prop.default` / `repack_recovery.py`
